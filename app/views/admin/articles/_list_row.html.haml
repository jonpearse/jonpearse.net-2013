%tr{ :id => "article-#{content.id}" }
  %th{ :scope => 'row' }
    = link_to content.title, edit_admin_article_path(content)
    %br
    %small= '/'+content.full_url
  %td=  content.created_on.strftime('%-e %b, %Y')
  %td.small
    - if content.published
      %span.icon.-ok.bool{ :title => 'yes' }
    - else
      %span.icon.-remove.bool{ :title => 'no' }
  %td.act
    = link_to '<span class="a11y">edit</span>'.html_safe, edit_admin_article_path(content), :class => 'icon -edit'
    = link_to '<span class="a11y">delete</span>'.html_safe, admin_article_path(content), :class => 'icon -trash', :method => :delete, :data => { :confirm => 'Are you sure you wish to delete this? It cannot be undone' }